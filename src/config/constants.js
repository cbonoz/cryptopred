// Constants and config variables.
export const WEB_WIDTH = 768; // min width before mobile css styles are used.
export const CORRECT_POINTS = 1; // number of points for a correct chart guess.
export const NUM_OPTIONS = 4; // number of options to show when the user makes a coin guess for the chart window.
export const MAX_LEADERBOARD_USERS = 100; // max number of leaderboard entries to show in the table.
